用途：将时空编码到长度为16个字母的字符串上，字母的取值范围为0-9+a-z，共32个字符
原理：
	如果将时空在三维上表示，每一维用一个二进制数表示那么
	`
	精度：11100101....
	纬度：10011010....
	时间：11010100....
	`
	那么将他们按经纬度时间三元组组合,每一个组合都包含一位经纬度时间将得到如下的串111,101,100,011,010,101,010,100...
	通过这个串将能够表示一个三维的盒子空间，这个空间盒子内是在时间和空间上相近的

	因为一共可以有32个字符可以使用，那么每个字符将最大能包含5位二进制数，16个字符的串，最大可以表示80位二进制数
	其中：
	`
	精度：27位
	纬度：27位
	时间：26位
	`
	那么最大划分下：
		精度被划分为：360.0 / (2**27)
		纬度被划分为：180.0 / (2**27)
		时间被划分为：自定义标尺 / (2**26)

	因为时间不像经纬度一样是一个恒定大小，所以必须选取一个窗口作为划分，根据不同的应用来选择
